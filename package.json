{
  "name": "meal-log-bot",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "ts-node ./tasks/run-build.ts",
    "build:dev": "ts-node ./tasks/run-build.ts -d",
    "start": "pm2-runtime application.json",
    "start:dev": "pm2 start application-dev.json",
    "start:demo": "nodemon ./dist/index.js --watch ./dist",
    "restart": "pm2 restart meal-log",
    "reload": "pm2 reload meal-log",
    "stop": "pm2 stop meal-log",
    "delete": "pm2 delete meal-log",
    "log": "pm2 log meal-log",
    "pm2": "pm2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/mongodb": "^3.6.9",
    "@types/node": "^14.14.33",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "mongodb": "^3.6.4",
    "pm2": "^4.5.5",
    "telegraf": "^4.2.1",
    "telegraf-session-mongodb": "^2.0.1"
  }
}
